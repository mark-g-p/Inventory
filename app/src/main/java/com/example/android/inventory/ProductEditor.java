package com.example.android.inventory;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

import com.example.android.inventory.R;

public class ProductEditor extends AppCompatActivity {
//TODO Using Pets app example write editor activity. Maybe later allow user to choose supplier from a list
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_product_editor);
    }
}
